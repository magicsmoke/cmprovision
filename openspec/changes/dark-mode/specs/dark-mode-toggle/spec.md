## ADDED Requirements

### Requirement: Theme Toggle Control

The navigation bar provides a toggle for switching between light and dark themes.

#### Scenario: Toggle theme

- **WHEN** user clicks the dark mode toggle
- **THEN** the theme switches immediately (no page reload)
- **AND** the toggle visually reflects the current theme state
